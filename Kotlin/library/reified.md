###### reified（具体化）

```
内联Lamdbd表达式（Inlinable lambdas）只能在内联函数中或作为内联函数的参数，而非内联表达式在所有中操作，如储存到字段，传递等。
若一个内联函数没有任何的内联参数，并且也没有具体化类型参数（reified type parameters），编译器会抛出一个警告，
提示内联函数没有实际意义（若认为内联定义时又必要的，也可以忽略该警告）。         
```